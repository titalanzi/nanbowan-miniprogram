# 云存储权限配置说明

为了让所有用户都能看到创建比赛用户的头像，需要配置云存储的读写权限。

## 配置步骤

1. 打开微信开发者工具
2. 点击左侧工具栏的「云开发」按钮
3. 进入云开发控制台
4. 点击左侧菜单的「存储」
5. 点击「权限设置」
6. 将权限设置为：
   - **所有用户可读，仅创建者可写**
   
   或者使用更宽松的权限：
   - **所有用户可读，所有用户可写**（不推荐，仅用于测试）

## 权限配置建议

推荐权限配置：
```json
{
  "read": true,
  "write": "auth.openid == doc._openid"
}
```

这个配置的含义：
- `read: true` - 所有用户都可以读取文件
- `write: "auth.openid == doc._openid"` - 只有文件的创建者可以修改/删除文件

## 验证配置

配置完成后：
1. 用户A上传头像
2. 用户B打开活动页面
3. 用户B应该能看到用户A的头像

## 注意事项

- 如果权限配置不正确，其他用户将无法看到头像
- 云存储文件路径格式：`cloud://环境ID.云存储域名/avatars/文件名`
- 如果遇到问题，请检查云开发环境ID配置是否正确
